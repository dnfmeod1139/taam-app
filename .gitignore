# Secrets — 절대 커밋 금지
secrets/
**/firebase-service-account.json
*.keystore
*.jks
*.p8
*.p12
*.mobileprovision
.env
.env.*

# Supabase CLI 로컬 캐시
supabase/.temp/
supabase/.branches/

# 의존성
node_modules/

# 시안 캔버스 — 에디터 payload 가 2MB 다. design/build.mjs + seed 로 언제든 다시 만든다
design/taam-admin-structure.html
